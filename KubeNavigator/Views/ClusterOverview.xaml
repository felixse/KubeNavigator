<?xml version="1.0" encoding="utf-8" ?>
<UserControl
  x:Class="KubeNavigator.Views.ClusterOverview"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:clustermetrics="using:KubeNavigator.ViewModels.ClusterMetrics"
  xmlns:controls="using:CommunityToolkit.WinUI.Controls"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="using:KubeNavigator.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="using:KubeNavigator.ViewModels"
  mc:Ignorable="d"
>
  <Grid RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock
      Grid.Row="0"
      Margin="8,0,0,0"
      VerticalAlignment="Center"
      Style="{ThemeResource SubtitleTextBlockStyle}"
      Text="{x:Bind ViewModel.Title}"
    />
    <controls:SwitchPresenter
      Grid.Row="1"
      TargetType="vm:MetricsState"
      Value="{x:Bind ViewModel.MetricsState, Mode=OneWay}"
    >
      <controls:Case Value="Pending">
        <ProgressRing IsActive="True" />
      </controls:Case>
      <controls:Case Value="Available">
        <ItemsRepeater ItemsSource="{x:Bind ViewModel.Metrics}">
          <ItemsRepeater.Layout>
            <UniformGridLayout
              ItemsStretch="Fill"
              MaximumRowsOrColumns="4"
              MinColumnSpacing="16"
              MinItemWidth="250"
              MinItemHeight="132"
              Orientation="Horizontal"
            />
          </ItemsRepeater.Layout>
          <ItemsRepeater.ItemTemplate>
            <DataTemplate x:DataType="clustermetrics:ClusterMetricBaseViewModel">
              <local:ClusterMetricsCard ViewModel="{x:Bind}" />
            </DataTemplate>
          </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
      </controls:Case>
      <controls:Case Value="NotAvailable">
        <InfoBar
          IsOpen="True"
          IsClosable="False"
          Severity="Informational"
          Title="Metrics not available"
          Message="Metrics server is not available in this cluster."
        />
      </controls:Case>
    </controls:SwitchPresenter>
    <local:ItemListView Grid.Row="2" ViewModel="{x:Bind ViewModel.EventListViewModel, Mode=OneWay}" />
  </Grid>
</UserControl>
