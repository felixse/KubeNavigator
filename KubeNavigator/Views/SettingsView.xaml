<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="KubeNavigator.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:KubeNavigator.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid MinWidth="600" MaxWidth="1500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border
            Grid.Row="0"
            Height="64"
            Padding="8">
            <TextBlock
                VerticalAlignment="Center"
                Style="{ThemeResource SubtitleTextBlockStyle}"
                Text="Settings" />
        </Border>
        <SelectorBar Grid.Row="1" Margin="0,0,0,16">
            <SelectorBarItem IsSelected="True" Text="General" />
            <SelectorBarItem Text="About" />
        </SelectorBar>
        <StackPanel Grid.Row="2" Spacing="4">
            <controls:SettingsCard Header="Application Theme">
                <ComboBox x:Name="ThemeComboBox" />
            </controls:SettingsCard>
        </StackPanel>

    </Grid>

</UserControl>
